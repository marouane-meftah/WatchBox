export default function NumResults({ watched }) {
    return (
      // <p className="num-results">
      //   <strong>{watched.length}</strong>
      //   {watched.length === 0 || watched.length === 1
      //     ? " movie watched"
      //     : " movies watched"}
      // </p>
  
    <div className="wlcm">
     Welcome, Princess Aya ❤️
    </div>
    );
  }